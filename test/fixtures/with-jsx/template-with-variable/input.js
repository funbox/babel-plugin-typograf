const amount = 20;

<section>
  <span>{`обычный "текст" `}</span>;

  <p>{T`Сервис доступен только абонентам, которые оформили подписку на "Мурзилку".`}</p>

  <div>
    {T`Стоимость подписки ${amount} рублей в день.`}
  </div>
</section>
